syntax = "proto3";
option csharp_namespace = "EmailService";
package email;

service Email {
  rpc Send(EmailRequest) returns (EmailReply);
}

message EmailRequest {
  string to = 1;
  string subject = 2;
  string body = 3;
}

message EmailReply {
  bool success = 1;
  string message = 2;
}
